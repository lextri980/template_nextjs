import { Control } from 'react-hook-form';
import { TFormInput } from '../../definition';

/**
 * [Type] DatePickerCollapse component props
 */
export type TDatePickerCollapseProps = {
  /** React Hook Form control object */
  control: Control<TFormInput>;
};
